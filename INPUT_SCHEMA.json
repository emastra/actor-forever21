{
  "title": "Forever21 input schema",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "startUrls": {
      "title": "Start Urls",
      "type": "array",
      "editor": "stringList",
      "description": "List of URLs that will be scraped or crawled. It can be the main website URL, (sub-)category URLs or product URLs."
    },
    "maxItems": {
      "title": "Max items",
      "type": "integer",
      "minimum": 1,
      "description": "Limit of product items to be scraped"
    },
    "extendOutputFunction": {
      "title": "Extend output function",
      "type": "string",
      "editor": "javascript",
      "description": "A function which result will get merged with the default result. It must return an object.",
      "prefill": "($) => {\n    const result = {};\n    // Uncomment to add a title to the output\n    // result.title = $('title').text().trim();\n\n    return result;\n}",
      "sectionCaption": "Advanced options"
    },
    "proxyConfiguration": {
      "title": "Proxy configuration",
      "type": "object",
      "editor": "proxy",
      "description": "The best option is usually Automatic proxy, but you can also use your own proxies or no proxy. Proxy's country defaults to 'US'.",
      "prefill": { "useApifyProxy": true }
    }
  },
  "required": ["startUrls"]
}
